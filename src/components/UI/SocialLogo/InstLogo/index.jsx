import React from "react";
import logo from "../../../../assets/instagramIcon.svg";

const instLogo = () => {
    return (
        <a href="#">
            <img src={logo} alt="instlogo" />
        </a>
    );
};

export default instLogo;
